# Локальные переопределения деплоя. Инвентарь флота — в deploy-hosts.yaml (он в репозитории).
# ВАЖНО: прогон на подмножестве НЕ сертифицирует флот — `stable` не пишется, код возврата
# «частично», пропущенные хосты перечисляются. Иначе один обновлённый хост объявлял бы
# зелёным флот, где второй остался старым.
# GATES_HOSTS="local"
